<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trading KPI Dashboard - Nadex Strategy</title>
    
    <!-- Material Symbols (Outlined/Monochrome) -->
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.js"></script>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .header {
            background: white;
            border-radius: 8px;
            padding: 24px;
            margin-bottom: 24px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .header h1 {
            color: #1a237e;
            font-weight: 500;
            font-size: 32px;
            margin-bottom: 8px;
        }
        
        .metadata {
            color: #666;
            font-size: 14px;
            display: flex;
            gap: 24px;
            flex-wrap: wrap;
            margin-top: 12px;
        }
        
        .metadata-item {
            display: flex;
            align-items: center;
            gap: 6px;
        }
        
        .metadata-item .material-symbols-outlined {
            font-size: 18px;
            color: #1a237e;
        }
        
        .kpi-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 24px;
            margin-bottom: 24px;
        }
        
        .kpi-card {
            background: white;
            border-radius: 8px;
            padding: 24px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        
        .kpi-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
        }
        
        .kpi-card-header {
            display: flex;
            align-items: center;
            gap: 12px;
            margin-bottom: 16px;
        }
        
        .kpi-icon {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }
        
        .kpi-icon.success {
            background: #e8f5e9;
            color: #2e7d32;
        }
        
        .kpi-icon.primary {
            background: #e3f2fd;
            color: #1565c0;
        }
        
        .kpi-icon.warning {
            background: #fff3e0;
            color: #ef6c00;
        }
        
        .kpi-title {
            font-size: 14px;
            color: #666;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-weight: 500;
        }
        
        .kpi-value {
            font-size: 36px;
            font-weight: 300;
            color: #1a237e;
            margin-bottom: 8px;
        }
        
        .kpi-detail {
            font-size: 14px;
            color: #666;
            margin-top: 4px;
        }
        
        .kpi-detail-row {
            display: flex;
            justify-content: space-between;
            margin-top: 8px;
            padding-top: 8px;
            border-top: 1px solid #e0e0e0;
        }
        
        .positive {
            color: #2e7d32;
        }
        
        .negative {
            color: #c62828;
        }
        
        .chart-container {
            background: white;
            border-radius: 8px;
            padding: 24px;
            margin-bottom: 24px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        
        .chart-title {
            font-size: 18px;
            color: #1a237e;
            margin-bottom: 16px;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        .chart-title .material-symbols-outlined {
            font-size: 24px;
        }
        
        .material-symbols-outlined {
            font-variation-settings:
                'FILL' 0,
                'wght' 400,
                'GRAD' 0,
                'opsz' 24;
        }
        
        canvas {
            max-height: 400px;
        }
        
        .footer {
            text-align: center;
            color: white;
            font-size: 12px;
            margin-top: 24px;
            opacity: 0.9;
        }
        
        @media (max-width: 768px) {
            .kpi-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header Section -->
        <div class="header">
            <h1>Trading Performance Dashboard</h1>
            <p style="color: #666; margin-top: 4px;">Nadex RSI Strategy - Performance KPIs</p>
            <div class="metadata">
                <div class="metadata-item">
                    <span class="material-symbols-outlined">date_range</span>
                    <span><strong>Period:</strong> {{ date_start }} - {{ date_end }}</span>
                </div>
                <div class="metadata-item">
                    <span class="material-symbols-outlined">show_chart</span>
                    <span><strong>Total Trades:</strong> {{ "{:,}".format(total_trades) }}</span>
                </div>
                <div class="metadata-item">
                    <span class="material-symbols-outlined">trending_up</span>
                    <span><strong>Wins:</strong> {{ "{:,}".format(wins) }}</span>
                </div>
                <div class="metadata-item">
                    <span class="material-symbols-outlined">trending_down</span>
                    <span><strong>Losses:</strong> {{ "{:,}".format(losses) }}</span>
                </div>
                <div class="metadata-item">
                    <span class="material-symbols-outlined">schedule</span>
                    <span><strong>Generated:</strong> {{ generated_time }}</span>
                </div>
            </div>
        </div>
        
        <!-- KPI Cards -->
        <div class="kpi-grid">
            <!-- Win Rate Card -->
            <div class="kpi-card">
                <div class="kpi-card-header">
                    <div class="kpi-icon success">
                        <span class="material-symbols-outlined">emoji_events</span>
                    </div>
                    <div class="kpi-title">Win Rate</div>
                </div>
                <div class="kpi-value {{ win_rate_class }}">{{ "{:.2%}".format(win_rate) }}</div>
                <div class="kpi-detail">{{ "{:,}".format(wins) }} wins out of {{ "{:,}".format(total_trades) }} trades</div>
            </div>
            
            <!-- P&L Card -->
            <div class="kpi-card">
                <div class="kpi-card-header">
                    <div class="kpi-icon primary">
                        <span class="material-symbols-outlined">account_balance_wallet</span>
                    </div>
                    <div class="kpi-title">Profit & Loss</div>
                </div>
                <div class="kpi-value {{ net_pnl_class }}">${{ "{:,.2f}".format(net_pnl) }}</div>
                <div class="kpi-detail">Net P&L</div>
                <div class="kpi-detail-row">
                    <span>Gross P&L:</span>
                    <span class="{{ gross_pnl_class }}"><strong>${{ "{:,.2f}".format(gross_pnl) }}</strong></span>
                </div>
                <div class="kpi-detail-row">
                    <span>Commissions:</span>
                    <span class="negative"><strong>-${{ "{:,.2f}".format(commissions) }}</strong></span>
                </div>
            </div>
            
            <!-- Max Drawdown Card -->
            <div class="kpi-card">
                <div class="kpi-card-header">
                    <div class="kpi-icon warning">
                        <span class="material-symbols-outlined">trending_down</span>
                    </div>
                    <div class="kpi-title">Maximum Drawdown</div>
                </div>
                <div class="kpi-value negative">${{ "{:,.2f}".format(max_drawdown) }}</div>
                <div class="kpi-detail">Peak-to-trough decline</div>
                <div class="kpi-detail-row">
                    <span>Drawdown %:</span>
                    <span class="negative"><strong>{{ "{:.2f}".format(max_drawdown_pct) }}%</strong></span>
                </div>
                <div class="kpi-detail-row">
                    <span>Recovery Time:</span>
                    <span><strong>{{ recovery_days }} days</strong></span>
                </div>
            </div>
        </div>
        
        <!-- Cumulative P&L Chart -->
        <div class="chart-container">
            <div class="chart-title">
                <span class="material-symbols-outlined">timeline</span>
                Cumulative P&L Over Time
            </div>
            <canvas id="plChart"></canvas>
        </div>
        
        <!-- Drawdown Chart -->
        <div class="chart-container">
            <div class="chart-title">
                <span class="material-symbols-outlined">bar_chart</span>
                Drawdown Analysis
            </div>
            <canvas id="drawdownChart"></canvas>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            <p>Generated by Livewell Nadex Trading System | Commission: ${{ "{:.2f}".format(commission_per_contract) }}/contract</p>
        </div>
    </div>
    
    <script>
        // Real data from backtesting
        const dates = {{ dates_json | safe }};
        const cumulativePL = {{ cumulative_pnl_json | safe }};
        const drawdown = {{ drawdown_json | safe }};
        
        // Cumulative P&L Chart
        const plCtx = document.getElementById('plChart').getContext('2d');
        new Chart(plCtx, {
            type: 'line',
            data: {
                labels: dates,
                datasets: [{
                    label: 'Cumulative P&L ($)',
                    data: cumulativePL,
                    borderColor: '#1565c0',
                    backgroundColor: 'rgba(21, 101, 192, 0.1)',
                    borderWidth: 2,
                    fill: true,
                    tension: 0.4,
                    pointRadius: 0,
                    pointHoverRadius: 4
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: true,
                plugins: {
                    legend: {
                        display: true,
                        position: 'top'
                    },
                    tooltip: {
                        mode: 'index',
                        intersect: false,
                        callbacks: {
                            label: function(context) {
                                return 'P&L: $' + context.parsed.y.toFixed(2);
                            }
                        }
                    }
                },
                scales: {
                    x: {
                        display: true,
                        title: {
                            display: true,
                            text: 'Date'
                        },
                        ticks: {
                            maxTicksLimit: 12
                        }
                    },
                    y: {
                        display: true,
                        title: {
                            display: true,
                            text: 'Cumulative P&L ($)'
                        },
                        ticks: {
                            callback: function(value) {
                                return '$' + value.toFixed(0);
                            }
                        }
                    }
                },
                interaction: {
                    mode: 'nearest',
                    axis: 'x',
                    intersect: false
                }
            }
        });
        
        // Drawdown Chart (Bar/Column)
        const ddCtx = document.getElementById('drawdownChart').getContext('2d');
        new Chart(ddCtx, {
            type: 'bar',
            data: {
                labels: dates,
                datasets: [{
                    label: 'Drawdown ($)',
                    data: drawdown,
                    backgroundColor: 'rgba(198, 40, 40, 0.7)',
                    borderColor: '#c62828',
                    borderWidth: 1,
                    borderRadius: 2
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: true,
                plugins: {
                    legend: {
                        display: true,
                        position: 'top'
                    },
                    tooltip: {
                        mode: 'index',
                        intersect: false,
                        callbacks: {
                            label: function(context) {
                                return 'Drawdown: $' + context.parsed.y.toFixed(2);
                            }
                        }
                    }
                },
                scales: {
                    x: {
                        display: true,
                        title: {
                            display: true,
                            text: 'Date'
                        },
                        ticks: {
                            maxTicksLimit: 12
                        }
                    },
                    y: {
                        display: true,
                        title: {
                            display: true,
                            text: 'Drawdown from Peak ($)'
                        },
                        ticks: {
                            callback: function(value) {
                                return '$' + value.toFixed(0);
                            }
                        },
                        max: 0
                    }
                },
                interaction: {
                    mode: 'nearest',
                    axis: 'x',
                    intersect: false
                }
            }
        });
    </script>
</body>
</html>
